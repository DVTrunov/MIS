
Процедура ПередЗаписью(Отказ, РежимЗаписи, РежимПроведения)
	Если Ссылка.Пустая() Или Не ЗначениеЗаполнено(Статус) Тогда
		Статус = Перечисления.СтатусыОтправкиЗаказовНаИсследовние.Создан;
	КонецЕсли;
КонецПроцедуры
