<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <style>
        body { 
            margin: 0; 
            padding: 0; 
            display: flex; 
            justify-content: center; 
            align-items: center; 
            height: 100vh; 
            background: #fff; 
        }
        
        /* Спиннер загрузки */
        .loader {
            border: 4px solid #f3f3f3;
            border-top: 4px solid #0078D7;
            border-radius: 50%;
            width: 40px;
            height: 40px;
            animation: spin 1s linear infinite;
            position: absolute;
        }
        @keyframes spin { 0% { transform: rotate(0deg); } 100% { transform: rotate(360deg); } }
        
        /* Стили для картинки */
        img { 
            max-width: 100%; 
            max-height: 100%; 
            object-fit: contain; 
            opacity: 0; 
            transition: opacity 0.5s ease-in-out; 
            z-index: 1;
        }
        
        /* Класс, применяемый после загрузки */
        img.loaded { opacity: 1; }
    </style>
</head>
<body>
    <div class="loader" id="spinner"></div>
    
    <img id="myImage" src="[URL_КАРТИНКИ]" 
         onload="document.getElementById('spinner').style.display='none'; this.classList.add('loaded');" />
</body>
</html>