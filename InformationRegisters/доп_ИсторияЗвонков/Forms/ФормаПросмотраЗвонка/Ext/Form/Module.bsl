
#Область ОбработчикиСобытийФормы

&НаКлиенте
Процедура ПриОткрытии(Отказ)
	ДлительностьЗвонка = доп_ТелефонияКлиентСервер.ФорматироватьДлительностьЗвонкаСЛидНулями(Запись.ДлительностьЗвонка);
КонецПроцедуры

&НаКлиенте
Процедура ЗаписьРазговораОткрытие(Элемент, СтандартнаяОбработка)
	СтандартнаяОбработка = Ложь;
	стрПутьКЗаписи = Запись.ЗаписьРазговора;
	Если ЗначениеЗаполнено(стрПутьКЗаписи) Тогда
		доп_БитфонКлиент.ВоспроизвестиЗаписьРазговора(стрПутьКЗаписи);
	КонецЕсли;
КонецПроцедуры

&НаКлиенте
Процедура ОбработкаОповещения(ИмяСобытия, Параметр, Источник)
	Если ИмяСобытия = "БитИсторияЗвонков_СохранениеФайлаЗаписиРазговораЗавершение" Тогда
		Если ЗначениеЗаполнено(Параметр.Результат) Тогда
			данныеЗапись = Параметр.ПараметрОповещения;
			имяФайла = Параметр.Результат[0];
			данныеЗапись.Записать(имяФайла);
		КонецЕсли;
	КонецЕсли;
КонецПроцедуры

#КонецОбласти

#Область ОбработчикиКомандФормы

&НаКлиенте
Процедура СкачатьЗапись(Команда)
	
	Если НЕ ЗначениеЗаполнено(Запись.ЗаписьРазговора) Тогда
		Возврат;
	КонецЕсли;
	
	стрСервисЗаписейНаАТС = доп_АТСКлиент.ПутьСервисаЗаписейРазговоровНаАТС();
	индЗаголовокЗап = Найти(Запись.ЗаписьРазговора, стрСервисЗаписейНаАТС);
	Если индЗаголовокЗап = 0 Тогда
		доп_ТелефонияКлиент.ВывестиСообщение("Скачивание записи разговора возможно только для записей, хранящихся на ОЛИМП.АТС", ЭтаФорма);
		Возврат;
	КонецЕсли;
	
	данныеЗапись = Неопределено;
	стрОшибкаHTTP = "";
	загружено = доп_ТелефонияКлиентСервер.СкачатьПоHTTPS(Запись.ЗаписьРазговора, 900, Ложь, данныеЗапись, стрОшибкаHTTP);
	
	Если загружено Тогда
		длг = Новый ДиалогВыбораФайла(РежимДиалогаВыбораФайла.Сохранение);
		длг.ПолноеИмяФайла = доп_ТелефонияКлиент.ПолучитьИмяФайлаЗаписиСДатой(Запись.Дата) + ".mp3"; 
		длг.Фильтр = "Файл mp3 (*.mp3)|*.mp3";
		доп_ТелефонияКлиентПереопределяемый.ПоказВыборФайла(длг, "БитИсторияЗвонков_СохранениеФайлаЗаписиРазговораЗавершение", данныеЗапись);
	Иначе
		доп_ТелефонияКлиент.ВывестиСообщение(стрОшибкаHTTP, ЭтаФорма);
		доп_ТелефонияКлиент.ВывестиСообщение("Ошибка загрузки записи разговора", ЭтаФорма);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ВоспроизвестиЗаписьЧерезМонитор(Команда)
	
	Если НЕ ЗначениеЗаполнено(Запись.ЗаписьРазговора) Тогда
		Возврат;
	КонецЕсли;
	
	стрСервисЗаписейНаАТС = доп_АТСКлиент.ПутьСервисаЗаписейРазговоровНаАТС();
	индЗаголовокЗап = Найти(Запись.ЗаписьРазговора, стрСервисЗаписейНаАТС);
	Если индЗаголовокЗап = 0 Тогда
		доп_ТелефонияКлиент.ВывестиСообщение("Прослушивание записи разговора на стационарном телефоне возможно только для записей, хранящихся на ОЛИМП.АТС", ЭтаФорма);
		Возврат;
	КонецЕсли;
	
	доп_АТСКлиент.ОткрытьМониторВоспроизвестиЗаписьРазговора(Запись.ЗаписьРазговора);
	
КонецПроцедуры

#КонецОбласти

